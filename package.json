{
	"name": "tic-tac-toe-engine",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun build ./main.ts --outdir ./public --target browser --format iife",
		"dev": "bun build ./main.ts --outdir ./public --target browser --format iife --watch",
		"glt": "bun run generate-lookup-table.ts"
	},
	"dependencies": {
		"@types/bun": "^1.3.14"
	}
}
